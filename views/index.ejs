<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:300">
    <title>Weatherly</title>
</head>
<body>
    <div class="container">
        <fieldset>
            <form action="/" method="POST" autocomplete="off">
                <input type="text" name="city" id="city" placeholder="Enter a city" required class="ghost-input">
                <input type="submit" id="submit" value="Get weather" class="ghost-button">
            </form>
            <!-- this a ejs template -->
            <% if(weather !== null){
                %><p><%= weather %></p>
                <% } %> <% if(error !== null){
                    %>
                    <p><%= error %></p>
                    <% } %>
              
        </fieldset>
    </div>
</body>
</html>